import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.theloadbearingman.app',
  appName: 'The Load-Bearing Man',
  webDir: 'dist'
};

export default config;
